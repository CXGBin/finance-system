# 财务管理系统 — 开发文档

> 版本：v1.1  
> 日期：2026-06-15  
> 状态：已确认

---

## 一、项目概述

### 1.1 项目目标

开发一套前后端分离的企业级财务管理系统，支持模块化功能开关，可按需启用或关闭业务模块，满足不同规模企业的财务管理需求。

### 1.2 技术选型

| 层级 | 技术方案 | 说明 |
|------|---------|------|
| 前端框架 | React 18 + TypeScript | 前端 UI 框架 |
| UI 组件库 | Ant Design 5.x | 企业级 UI 组件 |
| 构建工具 | Vite 5.x | 快速构建与热更新 |
| 状态管理 | Zustand | 轻量级状态管理 |
| 路由 | React Router v6 | 前端路由管理 |
| HTTP 客户端 | Axios | API 请求 |
| 后端框架 | .NET 8 (ASP.NET Core) | 后端 API 框架 |
| ORM | SqlSugarCore | 轻量级 ORM |
| 数据库 | SQL Server 2019+ | 关系型数据库（单租户） |
| 依赖注入 | .NET 原生 DI + Autofac | 模块化 DI 管理 |
| 缓存 | Redis | 分布式缓存 |
| 文档 | Swagger / Knife4j | API 文档 |
| 日志 | Serilog | 结构化日志 |

### 1.3 架构原则

- **前后端分离**：前端通过 RESTful API 与后端交互，独立部署
- **模块化设计**：每个业务模块独立，通过开关控制启用/禁用
- **依赖注入**：所有服务通过 DI 容器管理，模块按需注册
- **分层架构**：Controller → Service → Repository，职责清晰
- **统一响应**：标准化 API 响应格式，统一错误处理

---

## 二、系统架构

### 2.1 整体架构图

```
┌─────────────────────────────────────────────────────────────┐
│                        前端 (React + antd)                     │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌───────┐ │
│  │ 账务管理 │ │ 报表中心 │ │ 预算管理 │ │ 审批流程 │ │ 系统管理│ │
│  └────┬────┘ └────┬────┘ └────┬────┘ └────┬────┘ └───┬───┘ │
│       └───────────┴───────────┴───────────┴───────────┘     │
│                         │ Axios HTTP                          │
└─────────────────────────┼────────────────────────────────────┘
                          │ RESTful API (HTTPS)
┌─────────────────────────┼────────────────────────────────────┐
│                        后端 (.NET 8 API)                       │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │              API Gateway / 中间件层                        │ │
│  │  (认证授权 · 日志 · 异常处理 · 模块开关拦截)                  │ │
│  └──────────────────────┬───────────────────────────────────┘ │
│                         │                                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐       │
│  │账务模块Svc│  │报表模块Svc│  │预算模块Svc│  │审批模块Svc│  ...│
│  └─────┬────┘  └─────┬────┘  └─────┬────┘  └─────┬────┘       │
│        └──────────────┴──────────────┴──────────────┘         │
│                         │ DI Container (Autofac)                │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │         Repository 层 (SqlSugar ORM)                     │ │
│  └──────────────────────┬───────────────────────────────────┘ │
│                         │                                      │
│              ┌──────────┴──────────┐                          │
│              │    SQL Server     │                          │
│              └─────────────────────┘                          │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 前端架构

```
finance-web/
├── public/
├── src/
│   ├── api/                    # API 接口封装
│   │   ├── request.ts          # Axios 实例与拦截器
│   │   ├── auth.ts             # 认证接口
│   │   ├── finance/            # 财务业务接口
│   │   │   ├── account.ts
│   │   │   ├── voucher.ts
│   │   │   ├── report.ts
│   │   │   └── ...
│   │   └── system.ts           # 系统管理接口
│   ├── assets/                 # 静态资源
│   ├── components/             # 通用组件
│   │   ├── AuthGuard.tsx       # 路由守卫
│   │   ├── ModuleGuard.tsx     # 模块开关守卫
│   │   ├── Layout/
│   │   └── ...
│   ├── hooks/                  # 自定义 Hooks
│   ├── modules/                # 业务模块（按模块划分）
│   │   ├── account/            # 账务管理模块
│   │   ├── report/             # 报表中心模块
│   │   ├── budget/             # 预算管理模块
│   │   ├── approval/           # 审批流程模块
│   │   ├── asset/              # 资产管理模块
│   │   ├── expense/            # 费用管理模块
│   │   ├── tax/                # 税务管理模块
│   │   └── system/             # 系统管理模块（核心，不可关闭）
│   ├── store/                  # 状态管理
│   │   ├── useAuthStore.ts
│   │   ├── useModuleStore.ts   # 模块开关状态
│   │   └── useAppStore.ts
│   ├── router/                 # 路由配置
│   │   ├── index.tsx
│   │   ├── routes.ts           # 路由表（含模块标识）
│   │   └── lazyModules.ts      # 动态加载模块
│   ├── types/                  # TypeScript 类型定义
│   ├── utils/                  # 工具函数
│   ├── App.tsx
│   └── main.tsx
├── .env
├── vite.config.ts
├── tsconfig.json
└── package.json
```

### 2.3 后端架构

```
Finance.Api/
├── Controllers/                # 控制器
│   ├── AuthController.cs
│   ├── SystemController.cs
│   ├── account/               # 账务模块控制器
│   │   ├── VoucherController.cs
│   │   ├── LedgerController.cs
│   │   └── AccountController.cs
│   ├── report/                # 报表模块控制器
│   │   ├── BalanceSheetController.cs
│   │   ├── IncomeStatementController.cs
│   │   └── CashFlowController.cs
│   ├── budget/                # 预算模块控制器
│   ├── approval/              # 审批模块控制器
│   ├── asset/                 # 资产模块控制器
│   ├── expense/               # 费用模块控制器
│   └── tax/                   # 税务模块控制器
│
├── Services/                  # 服务层（接口 + 实现）
│   ├── Interfaces/
│   └── Implementations/
│       ├── AccountService.cs
│       ├── VoucherService.cs
│       ├── ReportService.cs
│       └── ...
│
├── Repositories/              # 数据访问层
│   ├── BaseRepository.cs      # 泛型仓储基类
│   └── ...
│
├── Models/                    # 数据模型
│   ├── Entities/              # 数据库实体
│   │   ├── SysUser.cs
│   │   ├── SysRole.cs
│   │   ├── FmAccount.cs
│   │   ├── FmVoucher.cs
│   │   └── ...
│   ├── Dtos/                  # 数据传输对象
│   │   ├── Request/
│   │   └── Response/
│   └── Enums/
│
├── Infrastructure/            # 基础设施层
│   ├── Database/
│   │   ├── DbContext.cs       # SqlSugar 配置
│   │   └── DbConfig.cs
│   ├── DI/                    # 依赖注入注册
│   │   ├── ServiceCollectionExtensions.cs
│   │   ├── ModuleServiceExtensions.cs  # 模块化 DI
│   │   └── ModuleRegistry.cs           # 模块注册中心
│   ├── Middleware/            # 自定义中间件
│   │   └── ModuleEnabledMiddleware.cs
│   ├── Security/
│   │   ├── JwtHelper.cs
│   │   └── PasswordHelper.cs
│   └── Cache/
│       └── RedisCacheService.cs
│
├── Modules/                   # 模块定义（核心）
│   ├── IAppModule.cs          # 模块接口
│   ├── FinanceModule.cs       # 模块基类
│   ├── AccountModule.cs       # 账务模块定义
│   ├── ReportModule.cs        # 报表模块定义
│   ├── BudgetModule.cs        # 预算模块定义
│   ├── ApprovalModule.cs      # 审批模块定义
│   ├── AssetModule.cs         # 资产模块定义
│   ├── ExpenseModule.cs       # 费用模块定义
│   └── TaxModule.cs           # 税务模块定义
│
├── Configuration/             # 配置
│   ├── ModuleConfig.cs        # 模块开关配置
│   ├── JwtConfig.cs
│   └── DbConnectionConfig.cs
│
├── Program.cs
├── appsettings.json
└── Finance.Api.csproj
```

---

## 三、功能模块设计

### 3.1 模块总览

| 序号 | 模块名称 | 模块标识 | 说明 | 默认状态 |
|------|---------|---------|------|---------|
| 1 | 系统管理 | `system` | 用户/角色/权限/菜单/组织架构/字典/日志管理 | **始终开启** |
| 2 | 账务管理 | `account` | 会计科目、凭证管理、账簿查询、期末处理 | 开启 |
| 3 | 报表中心 | `report` | 资产负债表、利润表、现金流量表、自定义报表 | 开启 |
| 4 | 预算管理 | `budget` | 预算编制、预算执行、预算分析、预算调整 | **开启** |
| 5 | 审批流程 | `approval` | 审批模板、审批流程、审批任务、审批记录 | **开启** |
| 6 | 资产管理 | `asset` | 固定资产登记、折旧管理、资产盘点、资产处置 | **开启** |
| 7 | 费用管理 | `expense` | 费用报销、费用分摊、费用预算对比 | **开启** |
| 8 | 税务管理 | `tax` | 税种管理、纳税申报、发票管理、税务分析 | **开启** |

### 3.2 模块功能详述

#### 3.2.1 系统管理（核心模块，不可关闭）

模块标识：`system` | 默认状态：**核心模块，始终开启**

| 序号 | 功能 | 说明 |
|------|------|------|
| S-01 | 用户登录/登出 | JWT 认证，支持记住我 |
| S-02 | Token 刷新 | 无感刷新 Token |
| S-03 | 修改密码 | 用户自行修改密码 |
| S-04 | 用户管理 | 用户 CRUD、状态管理、重置密码 |
| S-05 | 角色管理 | 角色 CRUD、权限分配 |
| S-06 | 菜单管理 | 动态菜单配置，树形结构 |
| S-07 | 部门管理 | 组织架构树形管理 |
| S-08 | 岗位管理 | 部门下的岗位维护 |
| S-09 | 数据字典 | 系统下拉项、枚举值统一管理 |
| S-10 | 操作日志 | 用户操作审计日志 |
| S-11 | 模块管理 | 模块列表、开关状态管理 |
| S-12 | 系统配置 | 全局参数配置 |
| S-13 | 个人中心 | 用户信息查看与修改 |

#### 3.2.2 账务管理

模块标识：`account` | 默认状态：开启

| 序号 | 功能 | 说明 |
|------|------|------|
| A-01 | 会计科目管理 | 多级科目树 CRUD，科目体系维护（CAS标准，最多6级） |
| A-02 | 科目期初余额 | 各科目期初余额录入与试算平衡 |
| A-03 | 凭证录入 | 手工凭证录入，支持多借多贷 |
| A-04 | 凭证查询 | 凭证列表查询与详情查看 |
| A-05 | 凭证修改 | 草稿凭证修改 |
| A-06 | 凭证审核 | 凭证审核/反审核（四眼原则） |
| A-07 | 凭证作废 | 凭证作废处理 |
| A-08 | 凭证打印 | 凭证打印（PDF） |
| A-09 | 总账查询 | 各科目总账（期初/本期/期末） |
| A-10 | 明细账查询 | 各科目明细账（逐笔展示） |
| A-11 | 日记账查询 | 现金/银行日记账 |
| A-12 | 多栏式明细账 | 多栏式账簿查询 |
| A-13 | 科目汇总表 | 按期间汇总科目发生额 |
| A-14 | 会计期间管理 | 会计期间新建/关闭/反结账，年末自动创建下年度 |
| A-15 | 期末损益结转 | 自动生成损益结转凭证至本年利润 |
| A-16 | 期末结账 | 月末/年末结账（含前置检查、年结自动结转利润分配） |
| A-17 | 辅助核算 | 科目辅助核算（部门/项目/客户/供应商） |

#### 3.2.3 报表中心

模块标识：`report` | 默认状态：开启 | 依赖模块：`account`

| 序号 | 功能 | 说明 |
|------|------|------|
| R-01 | 资产负债表 | 自动生成，符合 CAS 格式 |
| R-02 | 利润表（损益表） | 自动生成，支持期间对比 |
| R-03 | 现金流量表 | 自动生成，支持直接法 |
| R-04 | 科目余额表 | 各科目余额汇总查询 |
| R-05 | 明细账表 | 各科目明细账导出 |
| R-06 | 自定义报表 | 用户自定义报表模板与生成 |
| R-07 | 报表导出 | Excel / PDF 导出 |
| R-08 | 多期对比分析 | 同一报表多期间对比展示 |

#### 3.2.4 预算管理

模块标识：`budget` | 默认状态：开启 | 依赖模块：`account`、`system`

| 序号 | 功能 | 说明 |
|------|------|------|
| B-01 | 预算项目设置 | 预算科目/项目/部门的维度定义 |
| B-02 | 预算编制 | 按维度编制年度/月度预算 |
| B-03 | 预算审批 | 预算编制审批流程（若审批模块开启） |
| B-04 | 预算执行跟踪 | 实时对比预算与实际发生额 |
| B-05 | 预算预警 | 超预算或接近预算阈值时预警 |
| B-06 | 预算分析 | 多维预算与实际对比分析报表 |
| B-07 | 预算调整 | 预算追加/调减申请与审批 |
| B-08 | 预算报表导出 | Excel/PDF 导出 |

#### 3.2.5 审批流程

模块标识：`approval` | 默认状态：开启 | 依赖模块：`system`

| 序号 | 功能 | 说明 |
|------|------|------|
| AP-01 | 审批模板管理 | 定义审批类型、审批节点、审批人规则 |
| AP-02 | 审批发起 | 关联业务单据发起审批 |
| AP-03 | 待办任务 | 当前用户的待审批任务列表 |
| AP-04 | 审批处理 | 审批通过/驳回/转办/加签 |
| AP-05 | 已办任务 | 已处理的审批任务列表 |
| AP-06 | 我的申请 | 当前用户发起的审批记录 |
| AP-07 | 审批详情 | 审批完整流程轨迹展示 |
| AP-08 | 审批催办 | 对超时未处理的节点发起催办 |
| AP-09 | 审批统计 | 审批数量、通过率、平均处理时长统计 |

#### 3.2.6 资产管理

模块标识：`asset` | 默认状态：开启 | 依赖模块：`account`

| 序号 | 功能 | 说明 |
|------|------|------|
| AS-01 | 资产分类管理 | 固定资产类别维护 |
| AS-02 | 资产卡片管理 | 资产登记/修改/查看 |
| AS-03 | 资产折旧管理 | 月度计提折旧（直线法/双倍余额递减法等），折旧凭证自动生成 |
| AS-04 | 资产变动管理 | 调拨/处置/报废 |
| AS-05 | 资产盘点 | 盘点单生成与差异处理 |
| AS-06 | 资产报表 | 资产台账、折旧汇总等 |
| AS-07 | 折旧方式配置 | 折旧方法与规则配置 |

#### 3.2.7 费用管理

模块标识：`expense` | 默认状态：开启 | 依赖模块：`account`、`system`、`approval`

| 序号 | 功能 | 说明 |
|------|------|------|
| E-01 | 费用类型管理 | 报销费用分类维护 |
| E-02 | 费用报销申请 | 员工在线提交报销单 |
| E-03 | 费用明细管理 | 报销单中的费用行明细 |
| E-04 | 报销审批流程 | 报销单审批（若审批模块开启） |
| E-05 | 报销付款 | 审批通过后的付款确认 |
| E-06 | 费用分摊 | 公共费用按规则分摊到部门/项目 |
| E-07 | 费用统计 | 按维度统计费用支出 |
| E-08 | 预算对比 | 费用与预算的实时对比 |
| E-09 | 费用报表导出 | Excel/PDF 导出 |

#### 3.2.8 税务管理

模块标识：`tax` | 默认状态：开启 | 依赖模块：`account`

| 序号 | 功能 | 说明 |
|------|------|------|
| T-01 | 税种维护 | 税种基本信息与税率配置 |
| T-02 | 纳税申报计算 | 根据账务数据自动计算应纳税额 |
| T-03 | 纳税申报表 | 生成/查看纳税申报表 |
| T-04 | 发票管理 | 进项/销项发票登记、查验与统计 |
| T-05 | 税务报表 | 税务汇总报表 |
| T-06 | 税务日历 | 申报截止日期提醒 |

---

## 四、模块开关机制

### 4.1 设计思路

模块开关机制是本系统的核心特性之一，通过配置驱动实现模块的动态启用/禁用，涉及后端 DI 注册、中间件拦截和前端路由守卫三个层面。

### 4.2 后端模块开关

#### 4.2.1 模块接口定义

```csharp
// Modules/IAppModule.cs
public interface IAppModule
{
    /// <summary>模块唯一标识</summary>
    string ModuleId { get; }
    
    /// <summary>模块显示名称</summary>
    string ModuleName { get; }
    
    /// <summary>模块描述</summary>
    string Description { get; }
    
    /// <summary>模块是否为核心模块（不可关闭）</summary>
    bool IsCore { get; }
    
    /// <summary>是否已启用</summary>
    bool IsEnabled { get; set; }
    
    /// <summary>模块依赖（需先启用的模块列表）</summary>
    string[] Dependencies { get; }
    
    /// <summary>注册模块服务到 DI 容器</summary>
    void RegisterServices(IServiceCollection services, IConfiguration config);
}
```

#### 4.2.2 模块基类

```csharp
// Modules/FinanceModule.cs
public abstract class FinanceModule : IAppModule
{
    public abstract string ModuleId { get; }
    public abstract string ModuleName { get; }
    public abstract string Description { get; }
    public virtual bool IsCore => false;
    public virtual string[] Dependencies => Array.Empty<string>();
    
    public bool IsEnabled { get; set; }
    
    public abstract void RegisterServices(
        IServiceCollection services, IConfiguration config);
}
```

#### 4.2.3 模块注册中心

```csharp
// Infrastructure/DI/ModuleRegistry.cs
public class ModuleRegistry
{
    private readonly List<IAppModule> _modules = new();
    
    public void RegisterModule(IAppModule module)
    {
        _modules.Add(module);
    }
    
    public IReadOnlyList<IAppModule> GetAllModules() => _modules.AsReadOnly();
    
    public bool IsModuleEnabled(string moduleId)
    {
        var module = _modules.FirstOrDefault(m => m.ModuleId == moduleId);
        return module?.IsEnabled ?? false;
    }
    
    public void EnableModule(string moduleId)
    {
        var module = _modules.FirstOrDefault(m => m.ModuleId == moduleId);
        if (module != null) module.IsEnabled = true;
    }
    
    public void DisableModule(string moduleId)
    {
        var module = _modules.FirstOrDefault(m => m.ModuleId == moduleId);
        if (module != null && !module.IsCore)
            module.IsEnabled = false;
    }
    
    /// <summary>
    /// 仅注册已启用模块的服务到 DI 容器
    /// </summary>
    public void RegisterEnabledModules(
        IServiceCollection services, IConfiguration config)
    {
        foreach (var module in _modules.Where(m => m.IsEnabled))
        {
            module.RegisterServices(services, config);
        }
    }
}
```

#### 4.2.4 模块开关配置

```json
// appsettings.json
{
  "ModuleConfig": {
    "Modules": [
      { "ModuleId": "system",  "IsEnabled": true },
      { "ModuleId": "account", "IsEnabled": true },
      { "ModuleId": "report",  "IsEnabled": true },
      { "ModuleId": "budget",  "IsEnabled": true },
      { "ModuleId": "approval","IsEnabled": true },
      { "ModuleId": "asset",   "IsEnabled": true },
      { "ModuleId": "expense", "IsEnabled": true },
      { "ModuleId": "tax",     "IsEnabled": true }
    ]
  }
}
```

#### 4.2.5 模块开关中间件

```csharp
// Infrastructure/Middleware/ModuleEnabledMiddleware.cs
public class ModuleEnabledMiddleware
{
    private readonly RequestDelegate _next;
    private readonly ModuleRegistry _moduleRegistry;
    
    public ModuleEnabledMiddleware(
        RequestDelegate next, ModuleRegistry moduleRegistry)
    {
        _next = next;
        _moduleRegistry = moduleRegistry;
    }
    
    public async Task InvokeAsync(HttpContext context)
    {
        var path = context.Request.Path.Value?.ToLower() ?? "";
        
        // 解析请求路径对应的模块标识
        var moduleId = ResolveModuleIdFromPath(path);
        
        if (moduleId != null && !_moduleRegistry.IsModuleEnabled(moduleId))
        {
            context.Response.StatusCode = StatusCodes.Status403Forbidden;
            await context.Response.WriteAsJsonAsync(new
            {
                code = 403,
                message = $"模块 [{moduleId}] 未启用，请联系管理员开启"
            });
            return;
        }
        
        await _next(context);
    }
    
    private string? ResolveModuleIdFromPath(string path)
    {
        // /api/account/xxx → account
        // /api/report/xxx  → report
        var segments = path.Split('/', StringSplitOptions.RemoveEmptyEntries);
        if (segments.Length >= 2 && segments[0] == "api")
            return segments[1];
        return null;
    }
}
```

### 4.3 前端模块开关

#### 4.3.1 模块状态管理

```typescript
// store/useModuleStore.ts
import { create } from 'zustand';

interface ModuleState {
  modules: {
    moduleId: string;
    moduleName: string;
    isEnabled: boolean;
    isCore: boolean;
  }[];
  enabledModules: string[];
  isModuleEnabled: (moduleId: string) => boolean;
  setModules: (modules: ModuleState['modules']) => void;
}

export const useModuleStore = create<ModuleState>((set, get) => ({
  modules: [],
  enabledModules: [],
  
  isModuleEnabled: (moduleId: string) => {
    return get().enabledModules.includes(moduleId);
  },
  
  setModules: (modules) => {
    set({
      modules,
      enabledModules: modules
        .filter((m) => m.isEnabled)
        .map((m) => m.moduleId),
    });
  },
}));
```

#### 4.3.2 路由模块守卫

```typescript
// components/ModuleGuard.tsx
import React from 'react';
import { Navigate } from 'react-router-dom';
import { useModuleStore } from '../store/useModuleStore';

interface ModuleGuardProps {
  moduleId: string;
  children: React.ReactNode;
}

export const ModuleGuard: React.FC<ModuleGuardProps> = ({
  moduleId,
  children,
}) => {
  const isModuleEnabled = useModuleStore((s) => s.isModuleEnabled(moduleId));
  
  if (!isModuleEnabled) {
    return (
      <div style={{ padding: 48, textAlign: 'center' }}>
        <h3>该功能模块未启用</h3>
        <p>请联系系统管理员开启此模块</p>
      </div>
    );
  }
  
  return <>{children}</>;
};
```

#### 4.3.3 路由配置示例

```typescript
// router/routes.ts
import { ModuleGuard } from '../components/ModuleGuard';
import { lazy } from 'react';

export const routes = [
  // 系统管理 - 核心模块，无需守卫
  {
    path: '/system',
    element: <SystemLayout />,
    children: [
      { path: 'user', element: <UserManagement /> },
      { path: 'role', element: <RoleManagement /> },
      { path: 'module', element: <ModuleManagement /> },
    ],
  },
  // 账务管理
  {
    path: '/account',
    element: (
      <ModuleGuard moduleId="account">
        <AccountLayout />
      </ModuleGuard>
    ),
    children: [
      { path: 'subject', element: <SubjectManagement /> },
      { path: 'voucher', element: <VoucherManagement /> },
      { path: 'ledger', element: <LedgerQuery /> },
    ],
  },
  // 报表中心
  {
    path: '/report',
    element: (
      <ModuleGuard moduleId="report">
        <ReportLayout />
      </ModuleGuard>
    ),
    children: [
      { path: 'balance-sheet', element: <BalanceSheet /> },
      { path: 'income-statement', element: <IncomeStatement /> },
      { path: 'cash-flow', element: <CashFlowStatement /> },
    ],
  },
  // 预算管理
  {
    path: '/budget',
    element: <ModuleGuard moduleId="budget"><BudgetLayout /></ModuleGuard>,
    children: [
      { path: 'plan', element: <BudgetPlan /> },
      { path: 'analysis', element: <BudgetAnalysis /> },
    ],
  },
  // ... 其他模块同理
];
```

### 4.4 开关变更流程

```
管理员修改模块状态
    ↓
后端更新 appsettings / 数据库配置
    ↓
后端返回 403 给已关闭模块的 API 请求
    ↓
前端登录时拉取模块状态列表
    ↓
前端路由守卫屏蔽已关闭模块的页面入口
    ↓
侧边栏菜单自动隐藏已关闭模块（动态渲染）
```

---

## 五、数据库设计

### 5.1 核心表结构

#### 5.1.1 模块管理表 (sys_module)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 主键 |
| module_id | varchar(50) UNIQUE | 模块标识（account/report/budget...） |
| module_name | varchar(100) | 模块名称 |
| description | varchar(500) | 模块描述 |
| is_enabled | tinyint(1) | 是否启用 0/1 |
| is_core | tinyint(1) | 是否核心模块 0/1 |
| sort_order | int | 排序号 |
| dependencies | varchar(500) | 依赖模块ID，逗号分隔 |
| created_time | datetime | 创建时间 |
| updated_time | datetime | 更新时间 |

#### 5.1.2 用户表 (sys_user)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 主键 |
| username | varchar(50) UNIQUE | 用户名 |
| password_hash | varchar(256) | 密码哈希 |
| real_name | varchar(50) | 姓名 |
| email | varchar(100) | 邮箱 |
| phone | varchar(20) | 手机号 |
| avatar | varchar(500) | 头像 |
| dept_id | bigint | 部门ID |
| status | tinyint | 状态（0禁用 1启用） |
| created_time | datetime | 创建时间 |
| updated_time | datetime | 更新时间 |

#### 5.1.3 角色表 (sys_role)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 主键 |
| role_name | varchar(50) | 角色名称 |
| role_code | varchar(50) UNIQUE | 角色编码 |
| description | varchar(200) | 描述 |
| status | tinyint | 状态 |
| sort_order | int | 排序 |
| created_time | datetime | 创建时间 |

#### 5.1.4 用户角色关联表 (sys_user_role)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 主键 |
| user_id | bigint FK | 用户ID |
| role_id | bigint FK | 角色ID |

#### 5.1.5 菜单权限表 (sys_menu)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 主键 |
| parent_id | bigint | 父级ID |
| menu_name | varchar(50) | 菜单名称 |
| menu_type | tinyint | 类型（1目录 2菜单 3按钮） |
| path | varchar(200) | 路由路径 |
| component | varchar(200) | 组件路径 |
| permission | varchar(100) | 权限标识 |
| icon | varchar(50) | 图标 |
| module_id | varchar(50) | 所属模块 |
| sort_order | int | 排序 |
| visible | tinyint(1) | 是否可见 |
| status | tinyint | 状态 |

#### 5.1.6 角色菜单关联表 (sys_role_menu)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 主键 |
| role_id | bigint FK | 角色ID |
| menu_id | bigint FK | 菜单ID |

#### 5.1.7 部门表 (sys_dept)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 主键 |
| parent_id | bigint | 父级ID |
| dept_name | varchar(50) | 部门名称 |
| sort_order | int | 排序 |
| leader | varchar(50) | 负责人 |
| status | tinyint | 状态 |

#### 5.1.8 会计科目表 (fm_account_subject)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 主键 |
| subject_code | varchar(20) | 科目编码（如 1001, 1002.01） |
| subject_name | varchar(100) | 科目名称 |
| parent_id | bigint | 父级科目ID |
| subject_level | int | 层级 |
| subject_type | tinyint | 类型（1资产 2负债 3权益 4收入 5费用） |
| balance_direction | tinyint | 余额方向（1借方 2贷方） |
| is_enabled | tinyint(1) | 是否启用 |
| is_cash | tinyint(1) | 是否现金科目（用于日记账） |
| is_bank | tinyint(1) | 是否银行科目 |
| auxiliary_type | varchar(50) | 辅助核算类型：空/department/project/customer |
| sort_order | int | 排序 |
| remark | varchar(500) | 备注

#### 5.1.9 会计凭证表 (fm_voucher)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 主键 |
| voucher_no | varchar(20) UNIQUE | 凭证编号 |
| voucher_date | date | 凭证日期 |
| period_id | bigint FK | 会计期间ID |
| voucher_type | tinyint | 凭证类型（1收 2付 3转） |
| abstract_text | varchar(500) | 摘要 |
| status | tinyint | 状态（0草稿 1已审核 2已作废） |
| total_debit | decimal(18,2) | 借方合计 |
| total_credit | decimal(18,2) | 贷方合计 |
| prepared_by | bigint FK | 制单人 |
| reviewed_by | bigint FK | 审核人 |
| reviewed_time | datetime | 审核时间 |
| created_time | datetime | 创建时间 |

#### 5.1.10 凭证分录表 (fm_voucher_entry)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 主键 |
| voucher_id | bigint FK | 凭证ID |
| summary | varchar(200) | 摘要 |
| subject_id | bigint FK | 科目ID（末级） |
| debit_amount | decimal(18,2) | 借方金额 |
| credit_amount | decimal(18,2) | 贷方金额 |
| auxiliary_id | bigint FK | 辅助核算ID（科目有辅助核算时） |
| auxiliary_type | varchar(50) | 辅助核算类型 |

#### 5.1.11 会计期间表 (fm_period)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 主键 |
| period_year | int | 年度 |
| period_month | int | 月份 |
| begin_date | date | 开始日期 |
| end_date | date | 结束日期 |
| is_closed | tinyint(1) | 是否结账 |
| closed_time | datetime | 结账时间 |
| closed_by | bigint FK | 结账人 |

#### 5.1.12 科目期初余额表 (fm_subject_balance)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 主键 |
| subject_id | bigint FK | 科目ID |
| period_id | bigint FK | 会计期间ID |
| begin_debit | decimal(18,2) | 期初借方 |
| begin_credit | decimal(18,2) | 期初贷方 |
| current_debit | decimal(18,2) | 本期借方发生 |
| current_credit | decimal(18,2) | 本期贷方发生 |
| end_debit | decimal(18,2) | 期末借方余额 |
| end_credit | decimal(18,2) | 期末贷方余额 |

#### 5.1.13 操作日志表 (sys_log)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 主键 |
| user_id | bigint FK | 操作人 |
| module | varchar(50) | 所属模块 |
| action | varchar(50) | 操作类型 |
| description | varchar(500) | 操作描述 |
| ip_address | varchar(50) | IP 地址 |
| request_url | varchar(500) | 请求 URL |
| request_body | text | 请求体 |
| response_code | int | 响应码 |
| duration_ms | int | 耗时(ms) |
| created_time | datetime | 操作时间 |

#### 5.1.14 岗位表 (sys_post)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 主键 |
| dept_id | bigint FK | 所属部门 |
| post_code | varchar(50) UNIQUE | 岗位编码 |
| post_name | varchar(100) | 岗位名称 |
| sort_order | int | 排序 |
| status | tinyint | 状态（0禁用 1启用） |
| remark | varchar(200) | 备注 |
| created_time | datetime | 创建时间 |
| updated_time | datetime | 更新时间 |

#### 5.1.15 数据字典类型表 (sys_dict_type)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 主键 |
| dict_name | varchar(100) | 字典名称 |
| dict_type | varchar(100) UNIQUE | 字典类型标识 |
| status | tinyint | 状态 |
| remark | varchar(200) | 备注 |
| created_time | datetime | 创建时间 |

#### 5.1.16 数据字典项表 (sys_dict_data)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 主键 |
| dict_type | varchar(100) | 字典类型标识 |
| dict_label | varchar(100) | 字典标签 |
| dict_value | varchar(100) | 字典值 |
| sort_order | int | 排序 |
| status | tinyint | 状态 |
| remark | varchar(200) | 备注 |

#### 5.1.17 系统配置表 (sys_config)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 主键 |
| config_group | varchar(50) | 配置分组 |
| config_key | varchar(100) UNIQUE | 配置键 |
| config_value | varchar(500) | 配置值 |
| config_name | varchar(100) | 配置名称 |
| remark | varchar(200) | 备注 |
| created_time | datetime | 创建时间 |

#### 5.1.18 辅助核算-项目表 (fm_aux_project)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 主键 |
| project_code | varchar(50) UNIQUE | 项目编码 |
| project_name | varchar(100) | 项目名称 |
| manager | varchar(50) | 项目负责人 |
| begin_date | date | 开始日期 |
| end_date | date | 结束日期 |
| status | tinyint | 状态 |
| created_time | datetime | 创建时间 |

#### 5.1.19 辅助核算-客户表 (fm_aux_customer)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 主键 |
| customer_code | varchar(50) UNIQUE | 客户编码 |
| customer_name | varchar(100) | 客户名称 |
| contact | varchar(50) | 联系人 |
| phone | varchar(20) | 联系电话 |
| address | varchar(200) | 地址 |
| tax_no | varchar(50) | 税号 |
| status | tinyint | 状态 |
| created_time | datetime | 创建时间 |

#### 5.1.20 辅助核算-供应商表 (fm_aux_supplier)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 主键 |
| supplier_code | varchar(50) UNIQUE | 供应商编码 |
| supplier_name | varchar(100) | 供应商名称 |
| contact | varchar(50) | 联系人 |
| phone | varchar(20) | 联系电话 |
| address | varchar(200) | 地址 |
| tax_no | varchar(50) | 税号 |
| bank_name | varchar(100) | 开户银行 |
| bank_account | varchar(50) | 银行账号 |
| status | tinyint | 状态 |
| created_time | datetime | 创建时间 |

#### 5.1.21 自定义报表模板表 (fm_report_template)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 主键 |
| template_name | varchar(100) | 模板名称 |
| template_type | tinyint | 模板类型 |
| layout_config | text | 布局配置 JSON |
| sort_order | int | 排序 |
| status | tinyint | 状态 |
| created_by | bigint | 创建人 |
| created_time | datetime | 创建时间 |

#### 5.1.22 预算年度表 (fm_budget_year)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 主键 |
| year | int | 预算年度 |
| status | tinyint | 状态（0草稿 1已审批 2执行中 3已关闭） |
| total_budget | decimal(18,2) | 年度总预算 |
| approved_by | bigint | 审批人 |
| approved_time | datetime | 审批时间 |
| created_time | datetime | 创建时间 |

#### 5.1.23 预算科目表 (fm_budget_subject)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 主键 |
| year_id | bigint FK | 预算年度ID |
| subject_id | bigint FK | 关联会计科目 |
| dept_id | bigint FK | 关联部门（可选） |
| annual_amount | decimal(18,2) | 年度预算金额 |
| created_time | datetime | 创建时间 |

#### 5.1.24 月度预算表 (fm_budget_monthly)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 主键 |
| budget_subject_id | bigint FK | 预算科目ID |
| month | int | 月份（1-12） |
| amount | decimal(18,2) | 月度预算金额 |
| created_time | datetime | 创建时间 |

#### 5.1.25 预算调整表 (fm_budget_adjust)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 主键 |
| year_id | bigint FK | 预算年度ID |
| subject_id | bigint FK | 预算科目ID |
| adjust_type | tinyint | 调整类型（1追加 2调减） |
| adjust_amount | decimal(18,2) | 调整金额 |
| reason | varchar(500) | 调整原因 |
| status | tinyint | 状态（0待审批 1已通过 2已驳回） |
| created_by | bigint | 申请人 |
| created_time | datetime | 创建时间 |

#### 5.1.26 预算预警表 (fm_budget_alert)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 主键 |
| budget_subject_id | bigint FK | 预算科目ID |
| alert_type | tinyint | 预警类型（1超预算 2接近阈值） |
| budget_amount | decimal(18,2) | 预算金额 |
| actual_amount | decimal(18,2) | 实际金额 |
| threshold | decimal(5,2) | 预警阈值（%） |
| status | tinyint | 状态（0未处理 1已处理） |
| created_time | datetime | 创建时间 |

#### 5.1.27 预算预警配置表 (fm_budget_alert_config)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 主键 |
| year_id | bigint FK | 预算年度ID |
| threshold | decimal(5,2) | 预警阈值（%） |
| notify_type | varchar(50) | 通知方式 |
| status | tinyint | 状态 |
| created_time | datetime | 创建时间 |

#### 5.1.28 审批模板表 (fm_approval_template)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 主键 |
| template_name | varchar(100) | 模板名称 |
| business_type | varchar(50) | 业务类型（expense/budget/asset等） |
| node_config | text | 审批节点配置 JSON |
| status | tinyint | 状态（0停用 1启用） |
| created_time | datetime | 创建时间 |

#### 5.1.29 审批实例表 (fm_approval_instance)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 主键 |
| template_id | bigint FK | 审批模板ID |
| business_type | varchar(50) | 业务类型 |
| business_id | bigint | 业务单据ID |
| applicant_id | bigint | 申请人 |
| title | varchar(200) | 审批标题 |
| status | tinyint | 状态（0审批中 1已通过 2已驳回 3已撤回） |
| current_node | int | 当前节点序号 |
| created_time | datetime | 创建时间 |
| finished_time | datetime | 完成时间 |

#### 5.1.30 审批任务表 (fm_approval_task)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 主键 |
| instance_id | bigint FK | 审批实例ID |
| node_index | int | 节点序号 |
| assignee_id | bigint | 处理人 |
| status | tinyint | 状态（0待处理 1已通过 2已驳回 3已转办） |
| opinion | varchar(500) | 审批意见 |
| processed_time | datetime | 处理时间 |
| created_time | datetime | 创建时间 |

#### 5.1.31 审批操作日志表 (fm_approval_log)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 主键 |
| instance_id | bigint FK | 审批实例ID |
| task_id | bigint FK | 审批任务ID |
| operator_id | bigint | 操作人 |
| action | varchar(50) | 操作类型（approve/reject/transfer/countersign/remind） |
| opinion | varchar(500) | 意见 |
| created_time | datetime | 操作时间 |

#### 5.1.32 资产分类表 (fm_asset_category)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 主键 |
| parent_id | bigint | 父级ID |
| category_name | varchar(100) | 分类名称 |
| depreciation_method | tinyint | 折旧方法（1直线法 2双倍余额递减法 3年数总和法） |
| default_life_years | int | 默认使用年限 |
| residual_rate | decimal(5,2) | 残值率（%） |
| sort_order | int | 排序 |
| status | tinyint | 状态 |
| created_time | datetime | 创建时间 |

#### 5.1.33 资产卡片表 (fm_asset_card)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 主键 |
| asset_code | varchar(50) UNIQUE | 资产编码 |
| asset_name | varchar(100) | 资产名称 |
| category_id | bigint FK | 资产分类ID |
| dept_id | bigint FK | 使用部门 |
| original_value | decimal(18,2) | 原值 |
| residual_rate | decimal(5,2) | 残值率 |
| life_years | int | 使用年限 |
| depreciation_method | tinyint | 折旧方法 |
| subject_id | bigint FK | 关联折旧科目 |
| purchase_date | date | 购入日期 |
| status | tinyint | 状态（0在用 1闲置 2已处置 3已报废） |
| created_time | datetime | 创建时间 |

#### 5.1.34 折旧明细表 (fm_asset_depreciation)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 主键 |
| asset_id | bigint FK | 资产ID |
| period_id | bigint FK | 会计期间 |
| depreciation_amount | decimal(18,2) | 本期折旧额 |
| accumulated_amount | decimal(18,2) | 累计折旧额 |
| net_value | decimal(18,2) | 净值 |
| voucher_id | bigint FK | 关联凭证ID（生成后） |
| created_time | datetime | 创建时间 |

#### 5.1.35 资产变动表 (fm_asset_change)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 主键 |
| asset_id | bigint FK | 资产ID |
| change_type | tinyint | 变动类型（1调拨 2处置 3报废） |
| change_date | date | 变动日期 |
| from_dept_id | bigint | 原部门 |
| to_dept_id | bigint | 新部门 |
| reason | varchar(500) | 变动原因 |
| status | tinyint | 状态（0待审批 1已完成） |
| created_by | bigint | 操作人 |
| created_time | datetime | 创建时间 |

#### 5.1.36 盘点单表 (fm_asset_inventory)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 主键 |
| inventory_no | varchar(50) UNIQUE | 盘点单号 |
| inventory_date | date | 盘点日期 |
| status | tinyint | 状态（0盘点中 1已完成） |
| created_by | bigint | 创建人 |
| created_time | datetime | 创建时间 |

#### 5.1.37 盘点明细表 (fm_asset_inventory_item)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 主键 |
| inventory_id | bigint FK | 盘点单ID |
| asset_id | bigint FK | 资产ID |
| book_quantity | int | 账面数量 |
| actual_quantity | int | 实盘数量 |
| diff_quantity | int | 差异数量 |
| remark | varchar(200) | 备注 |

#### 5.1.38 费用类型表 (fm_expense_type)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 主键 |
| type_name | varchar(100) | 费用类型名称 |
| type_code | varchar(50) UNIQUE | 类型编码 |
| default_subject_id | bigint FK | 默认关联会计科目 |
| sort_order | int | 排序 |
| status | tinyint | 状态 |
| created_time | datetime | 创建时间 |

#### 5.1.39 报销单表 (fm_expense_claim)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 主键 |
| claim_no | varchar(50) UNIQUE | 报销单号 |
| applicant_id | bigint FK | 申请人 |
| dept_id | bigint FK | 部门 |
| total_amount | decimal(18,2) | 报销总额 |
| status | tinyint | 状态（0草稿 1待审批 2已通过 3已驳回 4已付款 5已撤回 6已作废） |
| remark | varchar(500) | 备注 |
| approval_instance_id | bigint FK | 关联审批实例ID |
| payment_voucher_id | bigint FK | 关联付款凭证ID |
| created_time | datetime | 创建时间 |

#### 5.1.40 报销明细表 (fm_expense_claim_item)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 主键 |
| claim_id | bigint FK | 报销单ID |
| expense_type_id | bigint FK | 费用类型 |
| subject_id | bigint FK | 会计科目 |
| amount | decimal(18,2) | 金额 |
| invoice_no | varchar(50) | 发票号 |
| description | varchar(500) | 费用说明 |

#### 5.1.41 费用分摊单表 (fm_expense_allocate)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 主键 |
| allocate_no | varchar(50) UNIQUE | 分摊单号 |
| total_amount | decimal(18,2) | 分摊总额 |
| allocate_type | tinyint | 分摊类型（1按部门 2按项目） |
| subject_id | bigint FK | 会计科目 |
| status | tinyint | 状态（0待确认 1已确认） |
| created_by | bigint | 创建人 |
| created_time | datetime | 创建时间 |

#### 5.1.42 分摊明细表 (fm_expense_allocate_detail)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 主键 |
| allocate_id | bigint FK | 分摊单ID |
| target_id | bigint | 分摊目标（部门/项目ID） |
| target_type | varchar(20) | 目标类型（dept/project） |
| amount | decimal(18,2) | 分摊金额 |
| subject_id | bigint FK | 会计科目 |

#### 5.1.43 税种表 (fm_tax_type)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 主键 |
| tax_name | varchar(100) | 税种名称 |
| tax_code | varchar(50) UNIQUE | 税种编码（如 VAT/CIT/PIT） |
| declaration_cycle | tinyint | 申报周期（1月度 2季度 3年度） |
| status | tinyint | 状态 |
| remark | varchar(200) | 备注 |
| created_time | datetime | 创建时间 |

#### 5.1.44 税率表 (fm_tax_rate)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 主键 |
| tax_type_id | bigint FK | 税种ID |
| rate_name | varchar(100) | 税率名称 |
| tax_rate | decimal(5,2) | 税率（%） |
| effective_date | date | 生效日期 |
| expire_date | date | 失效日期（可为空表示长期有效） |
| status | tinyint | 状态 |

#### 5.1.45 纳税申报表 (fm_tax_declaration)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 主键 |
| tax_type_id | bigint FK | 税种ID |
| declaration_no | varchar(50) UNIQUE | 申报编号 |
| period_year | int | 年度 |
| period_month | int | 月份 |
| taxable_amount | decimal(18,2) | 应纳税所得额 |
| tax_amount | decimal(18,2) | 应纳税额 |
| status | tinyint | 状态（0草稿 1已申报 2已缴款） |
| submitted_time | datetime | 申报时间 |
| paid_time | datetime | 缴款时间 |
| created_time | datetime | 创建时间 |

#### 5.1.46 发票表 (fm_tax_invoice)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | bigint PK | 主键 |
| invoice_type | tinyint | 发票类型（1进项 2销项） |
| invoice_code | varchar(20) | 发票代码 |
| invoice_no | varchar(20) | 发票号码 |
| invoice_date | date | 开票日期 |
| amount | decimal(18,2) | 金额（不含税） |
| tax_rate | decimal(5,2) | 税率 |
| tax_amount | decimal(18,2) | 税额 |
| total_amount | decimal(18,2) | 价税合计 |
| seller_name | varchar(200) | 销方名称 |
| buyer_name | varchar(200) | 购方名称 |
| subject_id | bigint FK | 关联会计科目 |
| voucher_id | bigint FK | 关联凭证ID |
| is_verified | tinyint | 是否已认证（仅进项） |
| status | tinyint | 状态 |
| created_time | datetime | 创建时间 |

---

## 六、接口规范

### 6.1 统一响应格式

```json
// 成功响应
{
  "code": 200,
  "message": "success",
  "data": { ... },
  "timestamp": 1718400000000
}

// 分页响应
{
  "code": 200,
  "message": "success",
  "data": {
    "list": [ ... ],
    "total": 100,
    "pageIndex": 1,
    "pageSize": 20
  },
  "timestamp": 1718400000000
}

// 失败响应
{
  "code": 400,
  "message": "具体错误信息",
  "data": null,
  "timestamp": 1718400000000
}
```

### 6.2 接口命名规范

| 操作 | HTTP Method | URL 格式 | 示例 |
|------|------------|---------|------|
| 新增 | POST | `/api/{module}/{resource}` | `POST /api/account/voucher` |
| 修改 | PUT | `/api/{module}/{resource}/{id}` | `PUT /api/account/voucher/1` |
| 删除 | DELETE | `/api/{module}/{resource}/{id}` | `DELETE /api/account/voucher/1` |
| 查询详情 | GET | `/api/{module}/{resource}/{id}` | `GET /api/account/voucher/1` |
| 分页查询 | GET | `/api/{module}/{resource}/page` | `GET /api/account/voucher/page?pageIndex=1&pageSize=20` |
| 列表查询 | GET | `/api/{module}/{resource}/list` | `GET /api/account/subject/list` |
| 操作类 | POST | `/api/{module}/{resource}/{action}` | `POST /api/account/voucher/1/audit` |

### 6.3 认证方式

- **JWT Token** 认证，请求头携带 `Authorization: Bearer {token}`
- Token 有效期：2 小时，支持刷新 Token（7 天有效期）
- 登录接口：`POST /api/auth/login`
- 刷新接口：`POST /api/auth/refresh-token`

### 6.4 核心接口清单

#### 系统管理

| 接口 | 方法 | 描述 |
|------|------|------|
| `/api/auth/login` | POST | 用户登录 |
| `/api/auth/logout` | POST | 用户登出 |
| `/api/auth/refresh-token` | POST | 刷新 Token |
| `/api/auth/profile` | GET | 获取当前用户信息 |
| `PUT /api/system/user/password` | PUT | 修改密码 |
| `/api/system/user/page` | GET | 用户分页查询 |
| `/api/system/user/{id}` | GET | 用户详情 |
| `/api/system/user` | POST | 新增用户 |
| `/api/system/user/{id}` | PUT | 修改用户 |
| `/api/system/user/{id}` | DELETE | 删除用户 |
| `/api/system/user/{id}/status` | PUT | 切换状态 |
| `/api/system/user/{id}/reset-password` | PUT | 重置密码 |
| `/api/system/user/{id}/roles` | GET | 获取用户角色 |
| `/api/system/user/{id}/roles` | PUT | 分配用户角色 |
| `/api/system/role/page` | GET | 角色分页查询 |
| `/api/system/role/list` | GET | 角色下拉列表 |
| `/api/system/role/{id}` | GET | 角色详情 |
| `/api/system/role` | POST | 新增角色 |
| `/api/system/role/{id}` | PUT | 修改角色 |
| `/api/system/role/{id}` | DELETE | 删除角色 |
| `/api/system/role/{id}/menus` | GET | 获取角色已分配菜单 |
| `/api/system/role/{id}/menus` | PUT | 分配角色菜单权限 |
| `/api/system/menu/tree` | GET | 菜单树 |
| `/api/system/menu` | POST | 新增菜单 |
| `/api/system/menu/{id}` | PUT | 修改菜单 |
| `/api/system/menu/{id}` | DELETE | 删除菜单 |
| `/api/system/dept/tree` | GET | 部门树 |
| `/api/system/dept` | POST | 新增部门 |
| `/api/system/dept/{id}` | PUT | 修改部门 |
| `/api/system/dept/{id}` | DELETE | 删除部门 |
| `/api/system/post/page` | GET | 岗位分页查询 |
| `/api/system/post` | POST | 新增岗位 |
| `/api/system/post/{id}` | PUT | 修改岗位 |
| `/api/system/post/{id}` | DELETE | 删除岗位 |
| `/api/system/dict/type/list` | GET | 字典类型列表 |
| `/api/system/dict/type` | POST | 新增字典类型 |
| `/api/system/dict/type/{id}` | PUT | 修改字典类型 |
| `/api/system/dict/type/{id}` | DELETE | 删除字典类型 |
| `/api/system/dict/data/list` | GET | 字典项列表 |
| `/api/system/dict/data` | POST | 新增字典项 |
| `/api/system/dict/data/{id}` | PUT | 修改字典项 |
| `/api/system/dict/data/{id}` | DELETE | 删除字典项 |
| `/api/system/log/page` | GET | 操作日志分页查询 |
| `/api/system/log/{id}` | GET | 日志详情 |
| `/api/system/module/list` | GET | 模块列表（含开关状态） |
| `/api/system/module/{id}/toggle` | PUT | 切换模块开关 |
| `/api/system/config/list` | GET | 系统配置列表 |
| `/api/system/config` | POST | 新增配置 |
| `/api/system/config/{id}` | PUT | 修改配置 |

#### 账务管理

| 接口 | 方法 | 描述 |
|------|------|------|
| `/api/account/subject/tree` | GET | 科目树（启用状态） |
| `/api/account/subject/tree-all` | GET | 科目树（含停用） |
| `/api/account/subject/{id}` | GET | 科目详情 |
| `/api/account/subject` | POST | 新增科目 |
| `/api/account/subject/{id}` | PUT | 修改科目 |
| `/api/account/subject/{id}` | DELETE | 删除科目 |
| `/api/account/subject/{id}/status` | PUT | 启用/停用 |
| `/api/account/subject/import` | POST | Excel 批量导入 |
| `/api/account/subject/export` | GET | Excel 导出 |
| `/api/account/subject/balance/{periodId}` | GET | 期初余额查询 |
| `/api/account/subject/balance` | POST | 批量保存期初余额 |
| `/api/account/subject/balance/trial-balance` | GET | 试算平衡校验 |
| `/api/account/voucher/page` | GET | 凭证分页查询 |
| `/api/account/voucher/{id}` | GET | 凭证详情 |
| `/api/account/voucher` | POST | 新增凭证 |
| `/api/account/voucher/{id}` | PUT | 修改凭证（仅草稿） |
| `/api/account/voucher/{id}/audit` | POST | 审核凭证 |
| `/api/account/voucher/{id}/unaudit` | POST | 反审核 |
| `/api/account/voucher/{id}/void` | POST | 作废凭证 |
| `/api/account/voucher/{id}/print` | GET | 打印单张 |
| `/api/account/voucher/print-batch` | POST | 批量打印 |
| `/api/account/ledger/general` | GET | 总账查询 |
| `/api/account/ledger/detail` | GET | 明细账查询 |
| `/api/account/ledger/cash-journal` | GET | 现金日记账 |
| `/api/account/ledger/bank-journal` | GET | 银行日记账 |
| `/api/account/ledger/multi-column` | GET | 多栏式明细账 |
| `/api/account/ledger/subject-summary` | GET | 科目汇总表 |
| `/api/account/period/list` | GET | 期间列表（按年度） |
| `/api/account/period/init-year` | POST | 初始化年度期间 |
| `/api/account/period/current` | GET | 获取当前期间 |
| `/api/account/period/profit-transfer` | POST | 损益结转 |
| `/api/account/period/close` | POST | 期末结账 |
| `/api/account/period/unclose` | POST | 反结账 |
| `/api/account/auxiliary/{type}/list` | GET | 辅助核算项列表 |
| `/api/account/auxiliary/{type}` | POST | 新增辅助项 |
| `/api/account/auxiliary/{type}/{id}` | PUT | 修改辅助项 |
| `/api/account/auxiliary/{type}/{id}` | DELETE | 删除辅助项 |
| `/api/account/ledger/auxiliary-detail` | GET | 辅助核算明细账 |

#### 报表中心

| 接口 | 方法 | 描述 |
|------|------|------|
| `/api/report/balance-sheet` | GET | 查询资产负债表 |
| `/api/report/income-statement` | GET | 查询利润表 |
| `/api/report/cash-flow` | GET | 查询现金流量表 |
| `/api/report/subject-balance` | GET | 查询科目余额表 |
| `/api/report/ledger-detail-export` | GET | 导出明细账 |
| `/api/report/custom/template/list` | GET | 模板列表 |
| `/api/report/custom/template` | POST | 创建模板 |
| `/api/report/custom/template/{id}` | PUT | 修改模板 |
| `/api/report/custom/template/{id}` | DELETE | 删除模板 |
| `/api/report/custom/template/{id}/generate` | GET | 按模板生成报表数据 |
| `/api/report/export/excel` | GET | Excel 导出 |
| `/api/report/export/pdf` | GET | PDF 导出 |
| `/api/report/compare` | GET | 多期对比 |

#### 预算管理

| 接口 | 方法 | 描述 |
|------|------|------|
| `/api/budget/setting/years` | GET | 预算年度列表 |
| `/api/budget/setting/year` | POST | 创建预算年度 |
| `/api/budget/setting/year/{id}/status` | PUT | 更新年度状态 |
| `/api/budget/setting/subjects/{yearId}` | GET | 预算科目列表 |
| `/api/budget/setting/subject` | POST | 新增预算科目 |
| `/api/budget/setting/subject/{id}` | PUT | 修改预算科目 |
| `/api/budget/setting/subject/{id}` | DELETE | 删除预算科目 |
| `/api/budget/plan/{yearId}` | GET | 查看年度预算编制 |
| `/api/budget/plan/monthly` | POST | 保存月度预算 |
| `/api/budget/plan/auto-split` | POST | 自动拆分年度预算到月度 |
| `/api/budget/plan/import` | POST | Excel 批量导入 |
| `/api/budget/plan/export` | GET | Excel 导出 |
| `/api/budget/plan/{yearId}/submit` | POST | 提交预算审批 |
| `/api/budget/plan/{yearId}/approve` | POST | 审批通过 |
| `/api/budget/plan/{yearId}/reject` | POST | 审批驳回 |
| `/api/budget/execution` | GET | 预算执行查询 |
| `/api/budget/alert/config` | GET | 获取预警配置 |
| `/api/budget/alert/config` | PUT | 修改预警配置 |
| `/api/budget/alert/list` | GET | 预警记录列表 |
| `/api/budget/alert/{id}/handle` | PUT | 标记预警已处理 |
| `/api/budget/analysis` | GET | 预算分析 |
| `/api/budget/adjust` | POST | 提交调整申请 |

#### 审批流程

| 接口 | 方法 | 描述 |
|------|------|------|
| `/api/approval/template/page` | GET | 模板分页查询 |
| `/api/approval/template/list` | GET | 模板下拉列表 |
| `/api/approval/template/{id}` | GET | 模板详情 |
| `/api/approval/template` | POST | 新增模板 |
| `/api/approval/template/{id}` | PUT | 修改模板 |
| `/api/approval/template/{id}` | DELETE | 删除模板 |
| `/api/approval/template/{id}/status` | PUT | 启用/停用 |
| `/api/approval/instance` | POST | 发起审批 |
| `/api/approval/instance/available-templates` | GET | 根据业务类型获取可用模板 |
| `/api/approval/pending/page` | GET | 待办任务分页 |
| `/api/approval/pending/count` | GET | 待办数量（首页徽标） |
| `/api/approval/{taskId}/approve` | POST | 通过 |
| `/api/approval/{taskId}/reject` | POST | 驳回 |
| `/api/approval/{taskId}/transfer` | POST | 转办 |
| `/api/approval/{taskId}/countersign` | POST | 加签 |
| `/api/approval/done/page` | GET | 已办任务分页 |
| `/api/approval/my/page` | GET | 我的申请分页 |
| `/api/approval/instance/{id}` | GET | 审批详情 |
| `/api/approval/instance/{id}/remind` | POST | 发起催办 |
| `/api/approval/statistics/overview` | GET | 审批概览统计 |

#### 资产管理

| 接口 | 方法 | 描述 |
|------|------|------|
| `/api/asset/category/tree` | GET | 分类树 |
| `/api/asset/category` | POST | 新增分类 |
| `/api/asset/category/{id}` | PUT | 修改分类 |
| `/api/asset/category/{id}` | DELETE | 删除分类 |
| `/api/asset/card/page` | GET | 资产卡片分页 |
| `/api/asset/card/{id}` | GET | 资产详情 |
| `/api/asset/card` | POST | 新增资产 |
| `/api/asset/card/{id}` | PUT | 修改资产 |
| `/api/asset/card/{id}` | DELETE | 删除资产 |
| `/api/asset/card/import` | POST | 批量导入 |
| `/api/asset/card/export` | GET | 导出 |
| `/api/asset/depreciation/calculate` | POST | 执行月度折旧计提 |
| `/api/asset/depreciation/generate-voucher` | POST | 生成折旧凭证 |
| `/api/asset/depreciation/preview/{period}` | GET | 预览某月折旧数据 |
| `/api/asset/depreciation/history/{assetId}` | GET | 单资产折旧明细 |
| `/api/asset/change/page` | GET | 变动记录分页 |
| `/api/asset/change` | POST | 提交变动申请 |
| `/api/asset/change/{id}/approve` | POST | 审批通过 |
| `/api/asset/change/history/{assetId}` | GET | 资产变动历史 |
| `/api/asset/inventory/page` | GET | 盘点单分页 |
| `/api/asset/inventory` | POST | 创建盘点单 |
| `/api/asset/inventory/{id}/complete` | POST | 完成盘点 |
| `/api/asset/report/list` | GET | 资产报表列表 |

#### 费用管理

| 接口 | 方法 | 描述 |
|------|------|------|
| `/api/expense/type/list` | GET | 费用类型列表 |
| `/api/expense/type` | POST | 新增费用类型 |
| `/api/expense/type/{id}` | PUT | 修改费用类型 |
| `/api/expense/type/{id}` | DELETE | 删除费用类型 |
| `/api/expense/claim/page` | GET | 报销单分页 |
| `/api/expense/claim/{id}` | GET | 报销单详情 |
| `/api/expense/claim` | POST | 新增报销单（草稿） |
| `/api/expense/claim/{id}` | PUT | 修改草稿 |
| `/api/expense/claim/{id}/submit` | POST | 提交审批 |
| `/api/expense/claim/{id}/cancel` | POST | 撤回报销单 |
| `/api/expense/claim/{id}/void` | POST | 作废报销单 |
| `/api/expense/claim/{id}/approve` | POST | 审批通过 |
| `/api/expense/claim/{id}/reject` | POST | 审批驳回 |
| `/api/expense/claim/{id}/pay` | POST | 确认付款 |
| `/api/expense/claim/pending-payment` | GET | 待付款列表 |
| `/api/expense/allocate/page` | GET | 分摊记录分页 |
| `/api/expense/allocate` | POST | 创建分摊单 |
| `/api/expense/allocate/{id}` | PUT | 修改分摊单 |
| `/api/expense/allocate/{id}/confirm` | POST | 确认分摊 |
| `/api/expense/allocate/{id}/generate-voucher` | POST | 生成分摊凭证 |
| `/api/expense/statistics` | GET | 费用统计 |

#### 税务管理

| 接口 | 方法 | 描述 |
|------|------|------|
| `/api/tax/tax-type/list` | GET | 税种列表 |
| `/api/tax/tax-type/{id}` | GET | 税种详情（含税率） |
| `/api/tax/tax-type` | POST | 新增税种 |
| `/api/tax/tax-type/{id}` | PUT | 修改税种 |
| `/api/tax/tax-type/{id}/rate` | POST | 新增税率 |
| `/api/tax/tax-rate/{rateId}` | PUT | 修改税率 |
| `/api/tax/declare/calculate` | POST | 计算应纳税额 |
| `/api/tax/declare/result/{declarationId}` | GET | 查看计算结果 |
| `/api/tax/declaration/page` | GET | 申报表分页 |
| `/api/tax/declaration/{id}` | GET | 申报表详情 |
| `/api/tax/declaration` | POST | 创建申报 |
| `/api/tax/declaration/{id}` | PUT | 修改申报数据 |
| `/api/tax/declaration/{id}/submit` | POST | 提交申报 |
| `/api/tax/declaration/{id}/pay` | POST | 确认缴款 |
| `/api/tax/declaration/export` | GET | 导出申报表 |
| `/api/tax/invoice/page` | GET | 发票分页查询 |
| `/api/tax/invoice/{id}` | GET | 发票详情 |
| `/api/tax/invoice` | POST | 登记发票 |
| `/api/tax/invoice/{id}` | PUT | 修改发票信息 |
| `/api/tax/invoice/{id}/verify` | POST | 认证进项发票 |
| `/api/tax/report` | GET | 税务报表 |
| `/api/tax/calendar` | GET | 税务日历 |

---

## 七、代码注释规范

> **强制要求**：所有代码必须包含完整注释，无注释的代码不予合并。

### 7.1 实体类注释

**SqlSugar 表级注释**：
```csharp
/// <summary>
/// 用户实体
/// </summary>
[SugarTable("sys_user", "系统用户表")]
public class SysUser
{
    /// <summary>
    /// 主键
    /// </summary>
    [SugarColumn(IsPrimaryKey = true, IsIdentity = true)]
    public long Id { get; set; }

    /// <summary>
    /// 用户名（唯一）
    /// </summary>
    [SugarColumn(Length = 50)]
    public string UserName { get; set; }

    /// <summary>
    /// 密码哈希
    /// </summary>
    [SugarColumn(Length = 256)]
    public string PasswordHash { get; set; }

    /// <summary>
    /// 状态（0禁用 1启用）
    /// </summary>
    public int Status { get; set; }
}
```

### 7.2 方法注释

**Service / Controller 方法**：
```csharp
/// <summary>
/// 根据ID获取用户详情
/// </summary>
/// <param name="id">用户ID</param>
/// <returns>用户详情DTO</returns>
public async Task<UserDto> GetByIdAsync(long id)
{
    // ...
}

/// <summary>
/// 审核凭证
/// </summary>
/// <param name="voucherId">凭证ID</param>
/// <param name="userId">审核人ID</param>
/// <exception cref="BusinessException">凭证不存在或状态不允许审核时抛出</exception>
public async Task AuditVoucherAsync(long voucherId, long userId)
{
    // ...
}
```

### 7.3 前端注释

- **复杂业务逻辑** 必须加注释说明意图
- **公共组件/Hook** 必须加 JSDoc 注释说明用途、参数、返回值
- **API 函数** 注释说明接口用途

```typescript
/**
 * 获取科目树（仅启用状态）
 * @param includeDisabled 是否包含停用科目，默认 false
 */
export const getSubjectTree = (includeDisabled = false) =>
  request.get<AccountSubject[]>('/api/account/subject/tree', {
    params: { includeDisabled },
  });
```

### 7.4 检查规则

- ✅ 每个实体类的每个属性必须有 `/// <summary>` 注释
- ✅ `[SugarTable]` 必须包含表名和表描述
- ✅ 每个方法必须有 `/// <summary>` 注释（含 param/returns/exception）
- ✅ 公共组件和 Hook 必须有 JSDoc
- ❌ 禁止无注释的方法提交
- ❌ 禁止无注释的实体属性提交

---

## 八、权限点清单

> 权限标识格式：`模块:资源:操作`，在菜单管理中配置，用于角色权限分配。

### 8.1 系统管理

| 权限标识 | 说明 |
|---------|------|
| system:user:list | 用户列表查看 |
| system:user:add | 新增用户 |
| system:user:edit | 修改用户 |
| system:user:delete | 删除用户 |
| system:user:reset-password | 重置密码 |
| system:role:list | 角色列表查看 |
| system:role:add | 新增角色 |
| system:role:edit | 修改角色 |
| system:role:delete | 删除角色 |
| system:menu:list | 菜单列表查看 |
| system:menu:add | 新增菜单 |
| system:menu:edit | 修改菜单 |
| system:menu:delete | 删除菜单 |
| system:dept:list | 部门列表查看 |
| system:dept:add | 新增部门 |
| system:dept:edit | 修改部门 |
| system:dept:delete | 删除部门 |
| system:post:list | 岗位列表查看 |
| system:post:add | 新增岗位 |
| system:post:edit | 修改岗位 |
| system:post:delete | 删除岗位 |
| system:dict:list | 字典查看 |
| system:dict:add | 新增字典 |
| system:dict:edit | 修改字典 |
| system:dict:delete | 删除字典 |
| system:log:list | 操作日志查看 |
| system:module:list | 模块管理查看 |
| system:module:edit | 模块开关操作 |
| system:config:list | 系统配置查看 |
| system:config:edit | 修改系统配置 |

### 8.2 账务管理

| 权限标识 | 说明 |
|---------|------|
| account:subject:list | 科目列表查看 |
| account:subject:add | 新增科目 |
| account:subject:edit | 修改科目 |
| account:subject:delete | 删除科目 |
| account:subject:import | 批量导入科目 |
| account:subject:export | 导出科目 |
| account:balance:view | 期初余额查看 |
| account:balance:edit | 期初余额录入 |
| account:voucher:list | 凭证列表查看 |
| account:voucher:add | 新增凭证 |
| account:voucher:edit | 修改凭证 |
| account:voucher:audit | 审核凭证 |
| account:voucher:unaudit | 反审核 |
| account:voucher:void | 作废凭证 |
| account:voucher:print | 打印凭证 |
| account:ledger:general | 总账查询 |
| account:ledger:detail | 明细账查询 |
| account:ledger:journal | 日记账查询 |
| account:ledger:summary | 科目汇总表 |
| account:period:view | 期间查看 |
| account:period:close | 期末结账 |
| account:period:unclose | 反结账 |
| account:period:transfer | 损益结转 |
| account:auxiliary:list | 辅助核算查看 |
| account:auxiliary:add | 新增辅助项 |
| account:auxiliary:edit | 修改辅助项 |

### 8.3 报表中心

| 权限标识 | 说明 |
|---------|------|
| report:balance-sheet:view | 资产负债表查看 |
| report:income-statement:view | 利润表查看 |
| report:cash-flow:view | 现金流量表查看 |
| report:subject-balance:view | 科目余额表查看 |
| report:custom:list | 自定义报表列表查看 |
| report:custom:add | 新增自定义报表 |
| report:custom:edit | 修改自定义报表 |
| report:custom:delete | 删除自定义报表 |
| report:export:excel | 导出 Excel |
| report:export:pdf | 导出 PDF |
| report:compare:view | 多期对比查看 |

### 8.4 预算管理

| 权限标识 | 说明 |
|---------|------|
| budget:setting:view | 预算设置查看 |
| budget:setting:edit | 预算设置修改 |
| budget:plan:view | 预算编制查看 |
| budget:plan:edit | 预算编制 |
| budget:execution:view | 预算执行查看 |
| budget:alert:view | 预警查看 |
| budget:analysis:view | 预算分析查看 |
| budget:adjust:list | 调整记录查看 |
| budget:adjust:add | 提交调整申请 |
| budget:export | 导出 |

### 8.5 审批流程

| 权限标识 | 说明 |
|---------|------|
| approval:template:list | 审批模板查看 |
| approval:template:add | 新增审批模板 |
| approval:template:edit | 修改审批模板 |
| approval:template:delete | 删除审批模板 |
| approval:instance:add | 发起审批 |
| approval:instance:view | 查看审批详情 |
| approval:statistics:view | 审批统计查看 |

### 8.6 资产管理

| 权限标识 | 说明 |
|---------|------|
| asset:category:list | 分类查看 |
| asset:category:add | 新增分类 |
| asset:category:edit | 修改分类 |
| asset:card:list | 资产列表查看 |
| asset:card:add | 新增资产 |
| asset:card:edit | 修改资产 |
| asset:card:delete | 删除资产 |
| asset:card:import | 批量导入 |
| asset:depreciation:view | 折旧查看 |
| asset:change:list | 变动记录查看 |
| asset:change:add | 提交变动 |
| asset:inventory:list | 盘点查看 |
| asset:inventory:add | 创建盘点 |
| asset:inventory:edit | 录入盘点结果 |
| asset:report:view | 资产报表查看 |
| asset:export | 导出 |

### 8.7 费用管理

| 权限标识 | 说明 |
|---------|------|
| expense:type:list | 费用类型查看 |
| expense:type:add | 新增费用类型 |
| expense:type:edit | 修改费用类型 |
| expense:claim:list | 报销单列表查看 |
| expense:claim:add | 提交报销 |
| expense:claim:edit | 修改报销单 |
| expense:claim:pay | 确认付款 |
| expense:allocate:list | 分摊查看 |
| expense:allocate:add | 创建分摊 |
| expense:statistics:view | 费用统计查看 |
| expense:export | 导出 |

### 8.8 税务管理

| 权限标识 | 说明 |
|---------|------|
| tax:type:list | 税种查看 |
| tax:type:add | 新增税种 |
| tax:type:edit | 修改税种/税率 |
| tax:declare:list | 申报表查看 |
| tax:declare:add | 创建申报 |
| tax:declare:edit | 修改申报 |
| tax:declare:pay | 确认缴款 |
| tax:invoice:list | 发票查看 |
| tax:invoice:add | 登记发票 |
| tax:invoice:edit | 修改发票 |
| tax:invoice:import | 批量导入发票 |
| tax:report:view | 税务报表查看 |
| tax:calendar:view | 税务日历查看 |
| tax:export | 导出 |

---

## 九、页面路由清单

### 9.1 系统管理

| 页面 | 路由 | 布局说明 |
|------|------|---------|
| 登录页 | `/login` | 独立页面，居中卡片式 |
| 用户管理 | `/system/user` | 表格 + 搜索 + 弹窗表单 |
| 角色管理 | `/system/role` | 表格 + 搜索 + 弹窗表单（含菜单权限树） |
| 菜单管理 | `/system/menu` | 左侧树 + 右侧表单 |
| 部门管理 | `/system/dept` | 左侧树 + 右侧列表 |
| 岗位管理 | `/system/post` | 表格 + 搜索 + 弹窗表单 |
| 字典管理 | `/system/dict` | 左侧字典类型 + 右侧字典项 |
| 操作日志 | `/system/log` | 表格 + 搜索（不可编辑） |
| 模块管理 | `/system/module` | 卡片列表 + 开关 Switch |
| 系统配置 | `/system/config` | 分组表单（Tab 页签分类） |
| 个人中心 | `/system/profile` | 个人信息编辑页 |

### 9.2 账务管理

| 页面 | 路由 | 布局说明 |
|------|------|---------|
| 会计科目管理 | `/account/subject` | 左侧科目树 + 右侧科目详情表单 |
| 期初余额 | `/account/balance` | 表格列表（科目+金额），顶部试算平衡校验按钮 |
| 凭证录入 | `/account/voucher/add` | 表头 + 分录明细表（动态增行），底部借贷合计 |
| 凭证查询 | `/account/voucher` | 顶部搜索栏 + 凭证列表表格 |
| 凭证详情 | `/account/voucher/:id` | 只读凭证卡片展示 |
| 总账 | `/account/ledger/general` | 科目选择器 + 期间选择 + 总账表格 |
| 明细账 | `/account/ledger/detail` | 科目多选 + 日期范围 + 明细账表格 |
| 日记账 | `/account/ledger/journal` | 科目选择（仅现金/银行）+ 逐笔表格 |
| 多栏式明细账 | `/account/ledger/multi-column` | 科目选择 + 多栏展示表格 |
| 科目汇总表 | `/account/ledger/summary` | 期间选择 + 汇总表格 |
| 会计期间 | `/account/period` | 按年度展示的12个月卡片，状态标识 |
| 期末处理 | `/account/period/close` | 引导式操作（损益结转→结账），展示前置检查项 |

### 9.3 报表中心

| 页面 | 路由 | 布局说明 |
|------|------|---------|
| 报表首页 | `/report` | 卡片入口（三大主表+科目余额+自定义） |
| 资产负债表 | `/report/balance-sheet` | 顶部条件栏（期间选择）+ 标准资产负债表布局 |
| 利润表 | `/report/income-statement` | 顶部条件栏（期间+本月/累计+对比）+ 利润表表格 |
| 现金流量表 | `/report/cash-flow` | 顶部条件栏 + 现金流量表（三大活动分组） |
| 科目余额表 | `/report/subject-balance` | 顶部条件栏 + 可展开折叠的科目余额表格 |
| 自定义报表列表 | `/report/custom` | 模板列表卡片/表格 |
| 自定义报表设计 | `/report/custom/edit/:id` | 行编辑器（行项目+科目选择+公式输入） |
| 自定义报表查看 | `/report/custom/view/:id` | 生成的报表数据表格 |
| 多期对比 | `/report/compare` | 报表类型选择 + 期间多选 + 对比表格/图表 |

### 9.4 预算管理

| 页面 | 路由 | 布局说明 |
|------|------|---------|
| 预算首页 | `/budget` | 预算执行概览（年度总预算/已执行/剩余，预警数） |
| 预算项目设置 | `/budget/setting` | 预算年度Tab + 预算科目表格 |
| 预算编制 | `/budget/plan` | 年度选择 + 科目-部门矩阵表格（含月度拆分） |
| 预算执行跟踪 | `/budget/execution` | 筛选栏 + 执行明细表格（含进度条/颜色预警） |
| 预算预警 | `/budget/alert` | 预警列表 + 配置入口 |
| 预算分析 | `/budget/analysis` | 分析维度Tab + 图表/表格切换 |
| 预算调整 | `/budget/adjust` | 调整申请表单 + 历史记录 |

### 9.5 审批流程

| 页面 | 路由 | 布局说明 |
|------|------|---------|
| 待办任务 | `/approval/pending` | 任务列表（高优先级置顶，超时标红） |
| 已办任务 | `/approval/done` | 已处理任务列表 |
| 我的申请 | `/approval/my` | 发起记录列表 |
| 审批详情 | `/approval/:id` | 基本信息卡片 + 审批流程时间线 + 操作按钮 |
| 审批模板管理 | `/approval/template` | 模板列表 + 模板编辑（节点拖拽排序） |
| 审批统计 | `/approval/statistics` | 统计卡片 + 图表 |

### 9.6 资产管理

| 页面 | 路由 | 布局说明 |
|------|------|---------|
| 资产分类 | `/asset/category` | 左侧分类树 + 右侧分类表单 |
| 资产列表 | `/asset/card` | 搜索栏 + 资产卡片表格 + 快捷操作 |
| 资产详情 | `/asset/card/:id` | 资产信息卡片 + 折旧明细Tab + 变动历史Tab |
| 折旧管理 | `/asset/depreciation` | 期间选择 + 折旧预览表格 + 计提按钮 |
| 资产变动 | `/asset/change` | 变动记录列表 + 新增变动弹窗 |
| 资产盘点 | `/asset/inventory` | 盘点单列表 + 盘点录入 |
| 资产报表 | `/asset/report` | Tab切换（台账/折旧/变动/价值） |

### 9.7 费用管理

| 页面 | 路由 | 布局说明 |
|------|------|---------|
| 费用首页 | `/expense` | 费用概览（本月报销总额/待审批/待付款）+ 快捷入口 |
| 报销申请 | `/expense/claim/add` | 报销表单 + 费用明细表（动态增行）+ 附件上传 |
| 报销列表 | `/expense/claim` | 搜索栏 + 报销单表格（状态Tag标识） |
| 报销详情 | `/expense/claim/:id` | 报销信息 + 费用明细 + 审批流程 + 操作按钮 |
| 待付款 | `/expense/payment` | 已通过待付款列表 + 付款确认弹窗 |
| 费用分摊 | `/expense/allocate` | 分摊单列表 + 分摊明细编辑 |
| 费用统计 | `/expense/statistics` | 统计维度Tab + 图表/表格 |
| 费用类型设置 | `/expense/type` | 费用类型表格 + 新增/编辑弹窗 |

### 9.8 税务管理

| 页面 | 路由 | 布局说明 |
|------|------|---------|
| 税务首页 | `/tax` | 税务概览（本月应纳税额、申报状态、预警数）+ 税务日历卡片 |
| 税种管理 | `/tax/type` | 税种列表 + 税率配置（抽屉面板） |
| 纳税申报 | `/tax/declaration` | 申报表列表（状态Tag）+ 创建申报向导 |
| 申报详情 | `/tax/declaration/:id` | 申报数据表格 + 操作按钮 |
| 发票管理 | `/tax/invoice` | Tab切换（进项/销项）+ 发票列表 + 新增发票弹窗 |
| 发票详情 | `/tax/invoice/:id` | 发票信息卡片 + 关联凭证 |
| 税务报表 | `/tax/report` | Tab切换（税负分析/增值税/企业所得税）+ 图表 |
| 税务日历 | `/tax/calendar` | 年度日历视图 + 截止日期标记 |

---

## 十、关键设计细节

### 10.1 依赖注入设计

```csharp
// Infrastructure/DI/ServiceCollectionExtensions.cs
public static class ServiceCollectionExtensions
{
    public static IServiceCollection AddFinanceSystem(
        this IServiceCollection services, IConfiguration configuration)
    {
        // 1. 注册基础设施
        services.AddSingleton<ModuleRegistry>();
        services.AddSqlSugarSetup(configuration);
        services.AddJwtAuth(configuration);
        services.AddRedisCache(configuration);
        services.AddSerilogLogging();
        
        // 2. 注册所有模块定义（不论开关）
        var moduleRegistry = services.BuildServiceProvider()
            .GetRequiredService<ModuleRegistry>();
        RegisterAllModules(moduleRegistry, configuration);
        
        // 3. 仅注册已启用模块的服务
        services.AddEnabledModuleServices(moduleRegistry, configuration);
        
        // 4. 注册通用服务（不受模块开关影响）
        services.AddScoped(typeof(IBaseRepository<>), typeof(BaseRepository<>));
        
        return services;
    }
    
    private static void RegisterAllModules(
        ModuleRegistry registry, IConfiguration config)
    {
        // 从配置读取模块状态
        var moduleConfigs = config
            .GetSection("ModuleConfig:Modules").Get<List<ModuleConfigDto>>();
        
        // 注册所有模块实例
        registry.RegisterModule(new SystemModule());
        registry.RegisterModule(new AccountModule());
        registry.RegisterModule(new ReportModule());
        registry.RegisterModule(new BudgetModule());
        registry.RegisterModule(new ApprovalModule());
        registry.RegisterModule(new AssetModule());
        registry.RegisterModule(new ExpenseModule());
        registry.RegisterModule(new TaxModule());
        
        // 同步配置中的开关状态
        foreach (var mc in moduleConfigs ?? Enumerable.Empty<ModuleConfigDto>())
        {
            var module = registry.GetAllModules()
                .FirstOrDefault(m => m.ModuleId == mc.ModuleId);
            if (module != null)
                module.IsEnabled = mc.IsEnabled;
        }
    }
    
    private static void AddEnabledModuleServices(
        IServiceCollection services, ModuleRegistry registry,
        IConfiguration config)
    {
        foreach (var module in registry.GetAllModules()
            .Where(m => m.IsEnabled))
        {
            module.RegisterServices(services, config);
        }
    }
}
```

### 10.2 统一响应封装

```csharp
public class ApiResponse<T>
{
    public int Code { get; set; }
    public string Message { get; set; } = "success";
    public T? Data { get; set; }
    public long Timestamp { get; set; } = DateTimeOffset.UtcNow.ToUnixTimeMilliseconds();
    
    public static ApiResponse<T> Ok(T data) => new() { Code = 200, Data = data };
    public static ApiResponse<T> Fail(string msg) => new() { Code = 500, Message = msg };
}

public class PagedResult<T>
{
    public List<T> List { get; set; } = new();
    public int Total { get; set; }
    public int PageIndex { get; set; }
    public int PageSize { get; set; }
}
```

### 10.3 泛型仓储基类

```csharp
public interface IBaseRepository<T> where T : class, new()
{
    Task<T> GetByIdAsync(long id);
    Task<List<T>> GetListAsync(Expression<Func<T, bool>> where);
    Task<PagedResult<T>> GetPageAsync(
        Expression<Func<T, bool>> where,
        int pageIndex, int pageSize,
        Expression<Func<T, object>> orderBy = null,
        bool isAsc = true);
    Task<T> InsertAsync(T entity);
    Task<bool> UpdateAsync(T entity);
    Task<bool> DeleteAsync(long id);
    Task<bool> DeleteAsync(Expression<Func<T, bool>> where);
}

public class BaseRepository<T> : IBaseRepository<T> where T : class, new()
{
    protected readonly ISqlSugarClient _db;
    
    public BaseRepository(ISqlSugarClient db)
    {
        _db = db;
    }
    
    public async Task<T> GetByIdAsync(long id)
    {
        return await _db.Queryable<T>().InSingleAsync(id);
    }
    
    // ... 其余方法实现
}
```

---

## 十一、开发里程碑

### 阶段一：基础框架搭建（第 1-2 周）

| 任务 | 说明 | 产出物 |
|------|------|--------|
| 后端项目初始化 | .NET 8 API 项目，NuGet 包引用 | 可运行 API 项目 |
| SqlSugar 配置 | 数据库连接、CodeFirst 初始化 | 数据库表结构 |
| 认证授权 | JWT 登录、Token 刷新、角色权限 | 登录/权限体系 |
| 前端项目初始化 | Vite + React + antd + TypeScript | 可运行前端项目 |
| 统一响应封装 | 后端统一响应 + 前端 Axios 拦截器 | 请求/响应规范 |

### 阶段二：系统管理 + 模块开关（第 3-4 周）

| 任务 | 说明 | 产出物 |
|------|------|--------|
| 系统管理模块 | 用户/角色/菜单/部门/字典/日志 CRUD | 系统管理页面 |
| 模块开关机制 | 后端 ModuleRegistry + 中间件 + 前端守卫 | 模块可开关 |
| 动态菜单 | 根据权限 + 模块开关渲染侧边栏 | 动态菜单 |

### 阶段三：账务管理核心（第 5-7 周）

| 任务 | 说明 | 产出物 |
|------|------|--------|
| 会计科目管理 | 多级科目树 CRUD | 科目管理页面 |
| 凭证管理 | 录入/审核/作废/查询 | 凭证管理页面 |
| 账簿查询 | 总账/明细账/日记账 | 账簿查询页面 |
| 期末处理 | 期初余额/损益结转/期末结账 | 期末处理功能 |

### 阶段四：报表中心（第 8-9 周）

| 任务 | 说明 | 产出物 |
|------|------|--------|
| 资产负债表 | 自动生成 | 报表页面 |
| 利润表 | 自动生成 | 报表页面 |
| 现金流量表 | 自动生成 | 报表页面 |
| 科目余额表 | 按期间查询 | 报表页面 |
| 自定义报表 | 模板配置 | 报表设计器 |

### 阶段五：业务模块开发（第 10-14 周）

> 全部 8 个模块默认开启，以下 5 个模块与阶段三、四并行或顺序开发。

| 任务 | 说明 | 产出物 |
|------|------|--------|
| 预算管理 | 编制/执行/分析/调整/预警 | 预算管理模块 |
| 审批流程 | 模板/流程/待办/催办/统计 | 审批管理模块 |
| 资产管理 | 分类/卡片/折旧/变动/盘点 | 资产管理模块 |
| 费用管理 | 报销/付款/分摊/统计/预算对比 | 费用管理模块 |
| 税务管理 | 税种/申报/发票/报表/税务日历 | 税务管理模块 |

### 阶段六：测试与交付（第 15-16 周）

| 任务 | 说明 | 产出物 |
|------|------|--------|
| 集成测试 | 全模块功能测试 | 测试报告 |
| 性能优化 | 接口响应/大数据量报表 | 优化报告 |
| 部署文档 | 部署流程与配置说明 | 部署手册 |
| 用户手册 | 功能操作指南 | 用户手册 |

---

## 十一、环境与部署要求

### 11.1 开发环境

| 工具 | 版本要求 |
|------|---------|
| .NET SDK | 8.0+ |
| Node.js | 18+ |
| pnpm | 8+ |
| SQL Server | 2019+ |
| Redis | 6.0+ |
| Visual Studio 2022 / Rider | 最新版 |

### 11.2 部署架构

```
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│   Nginx      │───▶│  前端静态文件  │    │  .NET API    │
│  (反向代理)   │    │  (build/dist) │    │  (Kestrel)   │
└──────┬───────┘    └──────────────┘    └──────┬───────┘
       │                                      │
       └──────────┐  ┌────────────────────────┘
                  │  │
           ┌──────┴──┴──────┐
           │ SQL Server/Redis │
           └───────────────┘
```

---

## 十二、已确认事项

以下决策已于 2026-06-15 由项目负责人确认：

1. ✅ **数据库选型**：SQL Server 2019+
2. ✅ **Redis 缓存**：启用（Token 存储和热点数据缓存）
3. ✅ **模块默认状态**：全部 8 个模块默认开启
4. ✅ **租户模式**：单租户
5. ✅ **会计准则**：中国大陆会计准则（CAS），完整企业财务体系（凭证/科目/结账）
6. ✅ **前端部署方式**：纯静态托管（Nginx）

---

> **所有关键决策已确认，从阶段一（基础框架搭建）开始开发。**
